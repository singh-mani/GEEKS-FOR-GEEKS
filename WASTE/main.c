#include <stdio.h>
#include <stdlib.h>
#include<unistd.h>
#include<sys/types.h>

int main()
{int count=0;
    if((spawn() )||( spawn() )&&( spawn()))
    {
        spawn();
    }
    printf("%d",count++);

    return 0;
}
